<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.srchhisto.dao.ISrchhistoDAO">

    <insert id="storeInsert" parameterType="com.project.srchhisto.Srchhisto">
        use srchhisto;
        insert ignore into ${memberId} (imageFileName, gpsAddress, storeName, storePhone, storeMenu, isBookmarked)
        values (#{imageFileName}, #{gpsAddress}, #{storeName}, #{storePhone}, #{storeMenu}, #{isBookmarked})
    </insert>

    <select id="storeSelect" parameterType="com.project.srchhisto.Srchhisto">
        use srchhisto;
        select * from ${memberId}
    </select>

    <select id="bookmarkedStoreSelect" parameterType="com.project.srchhisto.Srchhisto">
        use srchhisto;
        select * from ${memberId} where isBookmarked = 1;
    </select>

    <delete id="storeDelete" parameterType="com.project.srchhisto.Srchhisto">
        use srchhisto;
        delete from ${memberId} where imageFileName = #{imageFileName}
    </delete>

</mapper>