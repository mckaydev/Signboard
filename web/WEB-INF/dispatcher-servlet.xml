<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/mvc
       https://www.springframework.org/schema/mvc/spring-mvc.xsd
       http://www.springframework.org/schema/context
       https://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Annotation Activation -->
    <mvc:annotation-driven />
    <!-- Component Package Select -->
    <context:component-scan base-package="com.project" />

    <!-- mapping= 물리적 주소
        "/*" : 하위 폴더 까지만
        "/**" : 하위 단계 전부 다
         location= 가상 주소
         "/" : 리소스들이 저장되어 있는 상위 폴더 (web 폴더 전체 접근을 위해 "/"로 설정) -->
    <mvc:resources mapping="/img/**" location="/resources/img/" />
    <mvc:resources mapping="/resources/**" location="/resources/" />

    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/resources/jsp/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="50000000" />
        <property name="maxInMemorySize" value="50000000" />
    </bean>

<!--    <bean id="imageService" class="com.project.service.ImageService" />-->

    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="com.mysql.cj.jdbc.Driver" />
        <property name="url" value="jdbc:mysql://localhost:3306/signboard?allowMultiQueries=true&amp;serverTimezone=Asia/Seoul" />
        <property name="username" value="jutabi" />
        <property name="password" value="test1234" />
    </bean>

    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="configLocation" value="classpath:/mybatisConfig.xml"/>
<!--        <property name="mapperLocations" value="classpath:/mappers/*Mapper.xml"/>-->
    </bean>

    <bean id="sqlSession" name="sqlSession" class="org.mybatis.spring.SqlSessionTemplate" destroy-method="clearCache">
        <constructor-arg index="0" name="sqlSessionFactory" ref="sqlSessionFactory" />
    </bean>

</beans>