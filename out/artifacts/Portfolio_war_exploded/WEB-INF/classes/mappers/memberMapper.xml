<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.member.dao.IMemberDAO">

    <select id="memberRead" parameterType="com.project.member.Member" resultType="com.project.member.Member">
#         use signboard;
        select * from member where memberId = #{memberId} and memberPw = #{memberPw};
    </select>

    <insert id="memberCreate" parameterType="com.project.member.Member">
#         use signboard;
        insert into member (memberId, memberPw, memberEmail) values (#{memberId}, #{memberPw}, #{memberEmail});
#         use srchhisto;
#         create table ${memberId} (
#             imageFileName varchar(20),
#             gpsAddress varchar(20),
#             storeName varchar(20),
#             isBookmarked boolean default 0
#         );
    </insert>

    <update id="memberUpdate" parameterType="com.project.member.Member">
#         use signboard;
        update member set memberPw = #{memberPw},  memberEmail = #{memberEmail} where memberId = #{memberId};
    </update>

    <delete id="memberDelete" parameterType="com.project.member.Member">
#         use signboard;
        delete from member where memberId = #{memberId} and memberPw = #{memberPw} and memberEmail = #{memberEmail};
<!--        <if test="">-->
<!--            use srchhisto;-->
<!--            drop table ${memberId};-->
<!--        </if>-->
    </delete>
</mapper>